[package]
name = "clauvolution_body"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { workspace = true }
clauvolution_core = { path = "../clauvolution_core" }
clauvolution_genome = { path = "../clauvolution_genome" }
