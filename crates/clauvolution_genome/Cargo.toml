[package]
name = "clauvolution_genome"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
bevy = { workspace = true }
rand = { workspace = true }
rand_distr = { workspace = true }
clauvolution_core = { path = "../clauvolution_core" }
